// on submit function

const onSubmit = function {
    // get city

    // pass it to the validate function if...else

      // get from value
      //check if empty -> return true / false

}
// event listener(prevent default )




//fetch data function 
      // API (vedio 36min) have to call 2 API

         // construct my URL ${city}
         // fetch URL
         //then (handle response)
         //then (handle data)
                  //get lat lon name

                  //construct URL ($lat $lon)
                  //fetch (URL)
                  // handle response
                  //handle data (have current and forecast data)

         // after ICON code (vedio 53 min)         
         // for UV number check slack resources


//render current card
 // current object (mock data)
         //{- uv}  span class = ${getUv()}
         //{-icon}

         // getUvClassName (red /amber/green) use bootsrap Badge 
         
         //getIconUrl --> return "string" -> img. src attribute <img src=${getIconUrl}

         const renderForcastFunction = function{
             //map over forecast
             //callback
             //each 
                //{--------------} icon -> getIconUrl ->"--" img src
                //dt-->format-->""}
         }





//render current weather card function

// render forecast function 

//add to lS 

//render  search history

const renderSearchHistory = function {
    //get data from LS
    // map through the citites
    //in map call back function for each city construct
    // add click event listener 
    // join and append to parent 

//onload function call the function render search history



}





